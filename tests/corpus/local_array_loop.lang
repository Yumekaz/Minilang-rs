int ga[4];

func bump(int x) {
  return (x + ga[1]);
}

func main() {
  ga[1] = 3;
  int la[4];
  int i = 0;
  int acc = 0;
  while (i < 4) {
    la[i] = (acc + i);
    acc = (acc + la[i]);
    i = (i + 1);
  }
  return bump(acc);
}
